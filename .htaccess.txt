# Izinkan index.php dan bootstrap.inc.php
<FilesMatch "^(index|bootstrap\.inc)\.php$">
    Require all granted
</FilesMatch>

# Blokir file script berbahaya/eksekusi lainnya
<FilesMatch "(?i)\.(phtml|php[0-9]|sh|pl|cgi|asp|aspx|jsp|exe|shtml)$">
    Require all denied
</FilesMatch>

# Izinkan file tertentu
<FilesMatch "(?i)\.(jpg|jpeg|pdf|docx)$">
    Require all granted
</FilesMatch>

<IfModule mod_headers.c>
    Header set X-XSS-Protection "1; mode=block"
</IfModule>

Options -Indexes
```
